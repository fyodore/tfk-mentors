# Copy to .env.local for running Django on the host (outside Docker).
# Docker Compose uses the root .env file instead (DB_HOST=db).

DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
